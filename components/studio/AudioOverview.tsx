"use client"

import * as React from "react"

export function AudioOverview() {
  // No audio logic existed in the monolith; preserving behavior as-is.
  return (
    <div className="flex-1 overflow-y-auto p-4">
      <div className="rounded-lg border border-border bg-muted/20 p-6 text-sm text-muted-foreground">
        Audio overview is not configured yet.
      </div>
    </div>
  )
}
